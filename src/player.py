class Player():
    def __init__(self, name, player_id, symbol):
        x = 1
    """
    Functions required:
    Roll Dice
    Check Cards
    Make assumption (if in room)
    Select Square to move to
    Exit game
    """
